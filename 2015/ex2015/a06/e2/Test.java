package ex2015.a06.e2;

public class Test {

    /*
     * Scopo di questo esercizio è realizzare una GUI con l'aspetto mostrato nell'immagine fig.png fornita, 
     * che realizza una versione minimale del "gioco dell'oca" (a un solo giocatore), con una fila di pulsanti 
     * che rappresentano la scacchiera, e una fila di pulsanti tramite la quale l'utente può indicare l'esito
     * del tiro di un dado. La pedina (X) è inizialmente in prima posizione, e avanza di un numero di posizioni
     * pari al tiro del dado. La partita viene vinta se la pedina arriva in fondo uscendo dalle caselle 
     * consentite, è persa se si finisce sul pozzo (*). Appena vinta o persa, si mostri una Dialog e si chiuda 
     * l'applicazione.
     * 
     * Si prediligeranno (ma senza essere necessarie ai fini della sufficienza) le soluzioni che inseriscono
     * in una classe separata da GUI e ben incapsulata (pattern strategy) tutti gli aspetti del Model.
     * 
     * La classe GUIExample (la si esegua e se ne controlli il codice) fornisce codice facilmente adattabile
     * per gestire i componenti grafici necessari. 
     * 
     * * NOTA SULLA CONSEGNA
     * Mostrando la soluzione al docente, si produca compilazione ed esecuzione da linea di comando, a partire
     * da una medesima cartella -- pena decurtazione del punteggio.
     */

    public static void main(String[] args) throws java.io.IOException {
        //new GUI(20,12); // 20 caselle, pozzo in posizione 12
    }
}
