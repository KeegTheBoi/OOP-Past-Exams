package ex2014.a04.e2;

public class Test {
	
	/*
	 * Scopo di questo esercizio è realizzare una GUI con l'aspetto mostrato nell'immagine gui.png fornita, attraverso
	 * una classe FindMaxGUI con costruttore che accetta il numero di pulsanti che deve contenere (N) e il valore 
	 * massimo (escluso) dei numeri che posso essere mostrati (M). Il funzionamento della GUI è così espresso:
	 * 
	 * * All'avvio tutti gli N pulsanti indicano "?"
	 * * Viene generata una sequenza di N numeri random compresi fra 0 e M-1, ognuno "nascosto" sotto ad un pulsante
	 * * L'utente clicka un pulsante: se questo nasconde il numero più grande fra gli N, allora il numero viene mostrato
	 *   e il pulsante disabilitato, altrimenti non succede nulla
	 * * Successivamente, l'utente dovrà trovare il prossimo numero più grande fra quelli non ancora mostrati, e così via 
	 *   fino a trovarli tutti
	 * * Ad ogni click, lo standard output riporti il numero di tentativi finora effettuati
	 * * Quando tutti i numeri sono stati mostrati, l'applicazioni si chiuda
	 * 
	 * Si prediligeranno le soluzioni che incapsulano in un oggetto separato la gestione della generazione della 
	 * sequenza dei numeri e la politica con la quale decidere se un click deve portare o meno a mostrare il numero.
	 *    
	 * * NOTA SULLA CONSEGNA
	 * Mostrando la soluzione al docente, si produca compilazione ed esecuzione da linea di comando, a partire
	 * da una medesima cartella -- pena decurtazione del punteggio.
	 * 
	 */

	public static void main(String[] args) {
		//new FindMaxGUI(8,100);
	}

}
